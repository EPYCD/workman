# A Discord channel webhook. Leave empty and the watcher only logs — it will
# not fail, and `docker compose logs watch` still shows every check.
DISCORD_WEBHOOK_URL=
# Seconds between checks. Upstream moves a few times a day at most.
WATCH_INTERVAL=21600
